{
  "name": "dziennik-instruktora-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "MCP server for Dziennik Instruktora — exposes REST API as MCP tools for AI agents",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.11.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@vercel/node": "^5.0.0",
    "typescript": "^5.4.0"
  }
}
